[project]
name = "my_package"
version = "0.1.0"
description = "My minimal Python package"
authors = [{name="Your Name", email="your@email.com"}]
dependencies = []

[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"